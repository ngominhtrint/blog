<%= form_for [@article, Comment.new] do |f| %>
  <div class="form-line">
    <%= f.text_area :content, placeholder: "Comment...", class: 'form-control', required: true, data: { emojiable: true }, rows: 5 %>
    </br>
    <%= f.submit "Comment", class: "btn btn-success" %>
  </div>
<% end %>
